name: Deploy to Vercel (dev)

on:
  push:
    branches:
      - dev
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger Vercel Deployment
        run: curl -X POST "https://api.vercel.com/v1/integrations/deploy/prj_Hpmd5rR8bdZAIPcNrYW6J1FGiZj7/q4u0N6fCBC"

